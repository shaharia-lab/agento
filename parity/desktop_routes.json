{
  "_comment": [
    "Routes that exist ONLY in the desktop build. This is NOT a Go golden, and",
    "it is the one file in this directory that is not.",
    "",
    "read_routes.json and write_routes.json were generated by a chi.Walk over",
    "the router server.New built, and #391 froze them when the Go tree was",
    "deleted. Neither can record a route Go never had without ceasing to be",
    "what it is -- a record of Go's surface. Leaving the desktop-only routes in",
    "no file at all was the alternative, and it was rejected: those files exist",
    "so the route surface cannot drift silently, and a whole family recorded",
    "nowhere would weaken exactly that property.",
    "",
    "So this file is the third table, with the same row shape and one stronger",
    "guarantee. The two Go files are asserted in ONE direction -- the Rust test",
    "iterates their rows, so a route that is claimed and never recorded passes.",
    "Here the module under test exposes its routes as a single const (see",
    "native::security::ROUTES) and the assertion is set equality, so a route",
    "cannot be added, removed or renamed without this file moving.",
    "",
    "'status' is 'native' or 'dropped', as in read_routes.json. Nothing is",
    "'deferred': there is no second implementation left to defer to.",
    "",
    "Nothing regenerates this file. It is authored alongside the code, and the",
    "test is what keeps the two honest."
  ],
  "routes": [
    {
      "method": "GET",
      "route": "/api/security/keys",
      "status": "native",
      "owner": "#405",
      "reason": "the public key, its kid and where both files live; write-scoped, because it names the private key's path"
    },
    {
      "method": "POST",
      "route": "/api/security/keys/regenerate",
      "status": "native",
      "owner": "#405",
      "reason": "replaces the install's keypair, which refuses every token ever issued including the caller's own"
    },
    {
      "method": "GET",
      "route": "/api/security/tokens",
      "status": "native",
      "owner": "#405",
      "reason": "the issued tokens, revoked ones included; write-scoped, because it enumerates every credential on the machine"
    },
    {
      "method": "POST",
      "route": "/api/security/tokens",
      "status": "native",
      "owner": "#405",
      "reason": "mints a scoped token and returns the only copy of it that will ever exist"
    },
    {
      "method": "DELETE",
      "route": "/api/security/tokens/{id}",
      "status": "native",
      "owner": "#405",
      "reason": "revokes one token by jti; an UPDATE rather than a DELETE, because removing the row would un-revoke it"
    },
    {
      "method": "GET",
      "route": "/.well-known/jwks.json",
      "status": "native",
      "owner": "#405",
      "reason": "the public key in JWKS form, UNAUTHENTICATED by design -- it is what a credential is verified against, so requiring one would be a bootstrap problem with no answer"
    },
    {
      "method": "GET",
      "route": "/api/gateway/settings",
      "status": "native",
      "owner": "#426",
      "reason": "the enable toggle, the port and start-with-app; read-scoped, because it names no secret"
    },
    {
      "method": "PUT",
      "route": "/api/gateway/settings",
      "status": "native",
      "owner": "#426",
      "reason": "stores the three and reloads the listener; a port below 1024 is refused before anything is written"
    },
    {
      "method": "GET",
      "route": "/api/gateway/providers",
      "status": "native",
      "owner": "#426",
      "reason": "the configured providers through the PUBLIC projection -- api_key is a boolean computed in SQL, never a value"
    },
    {
      "method": "POST",
      "route": "/api/gateway/providers",
      "status": "native",
      "owner": "#426",
      "reason": "creates a provider with its API key; answers through the public projection, so the key it just stored is not echoed"
    },
    {
      "method": "PUT",
      "route": "/api/gateway/providers/{id}",
      "status": "native",
      "owner": "#426",
      "reason": "updates a provider; an OMITTED api_key preserves the stored one, which is what stops the PUT /api/integrations/{id} credential-wipe being reproduced here"
    },
    {
      "method": "DELETE",
      "route": "/api/gateway/providers/{id}",
      "status": "native",
      "owner": "#426",
      "reason": "deletes a provider, or 409s naming the aliases still routed to it -- routing refers to providers by name inside a JSON column, so no foreign key can catch this"
    },
    {
      "method": "GET",
      "route": "/api/gateway/models",
      "status": "native",
      "owner": "#426",
      "reason": "the model aliases and their ordered targets and fallbacks"
    },
    {
      "method": "POST",
      "route": "/api/gateway/models",
      "status": "native",
      "owner": "#426",
      "reason": "creates an alias; a target naming no configured provider is refused rather than stored to fail at request time"
    },
    {
      "method": "PUT",
      "route": "/api/gateway/models/{id}",
      "status": "native",
      "owner": "#426",
      "reason": "updates an alias; checks the row exists first, because store_alias upserts and would otherwise create silently on a PUT"
    },
    {
      "method": "DELETE",
      "route": "/api/gateway/models/{id}",
      "status": "native",
      "owner": "#426",
      "reason": "deletes an alias by row id, not by alias string -- nothing stops two rows sharing an alias"
    },
    {
      "method": "GET",
      "route": "/api/gateway/status",
      "status": "native",
      "owner": "#426",
      "reason": "running / stopped / bind_failed / start_failed plus the port and the reason; a STORED value, so a port collision is distinguishable from a gateway that is simply off"
    },
    {
      "method": "GET",
      "route": "/api/gateway/usage",
      "status": "native",
      "owner": "#426",
      "reason": "windowed aggregates over gateway_usage_log, bucketed in the request's timezone; unpriced requests are surfaced as a count, never folded into the cost total as zero"
    }
  ]
}
