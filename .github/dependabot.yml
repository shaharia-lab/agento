version: 2
updates:
  # Backend: Go modules — patch updates only, single grouped PR
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 1
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
    groups:
      backend:
        patterns:
          - "*"

  # Frontend: npm packages — patch updates only, single grouped PR
  - package-ecosystem: npm
    directory: /frontend
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 1
    # typescript-eslint does not support TS 7 yet; eslint-plugin-react-hooks 7.1
    # introduces stricter rules that need a dedicated cleanup pass
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
      - dependency-name: typescript
        versions: [">=7.0.0"]
      - dependency-name: eslint-plugin-react-hooks
        versions: [">=7.1.0"]
    groups:
      frontend:
        patterns:
          - "*"

  # GitHub Actions — single grouped PR (all actions are pinned to commit SHAs)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 1
    groups:
      actions:
        patterns:
          - "*"
